<Page
    x:Class="VisitorApp.Tester"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VisitorApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:VisitorApp.Common"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <!--<GridView ItemsSource="{Binding Guest}">
            <GridView.ItemTemplate>
                <DataTemplate data="Tester">
                    <StackPanel>
                        <Image Width="150" Source="{Binding ImagePath}"/>
                        <TextBlock FontSize="16" Text="{Binding GuestName}"/>
                        <TextBlock FontSize="10" Text="{Binding HostName}"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>-->
        
        <TextBlock
            Name="ResultTextBlock"
            FontSize="24"
            Foreground="Red"
            FontWeight="Bold"
            Margin="0,20,0,0"
            />

    </Grid>
</Page>
